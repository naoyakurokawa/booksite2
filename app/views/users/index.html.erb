<h1>Users Index</h1>
<%= link_to 'Add user', new_user_path %>

<ul>
  <% @users.each do |user| %>
    <li>
      <p><%= user.name %></p>
      <%= image_tag user.avatar.variant(resize: '200x200') %>
      <%= link_to("詳細を見る", "/users/#{user.id}") %>
    </li>
  <% end %>
</ul>